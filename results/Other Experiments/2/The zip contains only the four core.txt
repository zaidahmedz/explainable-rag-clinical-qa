The zip contains only the four core files — no eval_norag.json or eval_minilm.json, so the baselines haven't been run yet. This is the main BioBERT run. Let me check whether it's the good run or something changed.Analyze the current eval results and corpusAnalyze the current eval results and corpusThis confirms your good run is solid and correctly built. The corpus is now right: 2385 chunks across all 1000 PMIDs, with both PQA-L-test (1187 chunks) and PQA-L-train (1198 chunks) present. That's the fix working — the test abstracts are in the corpus, which is why hit rate is 0.88 instead of 0.
So this is the same 0.532 result you reported, and I can now confirm from the raw data that it's legitimate, not a fluke or a bug:
Accuracy 0.532, macro-F1 0.472, hit rate 0.880, faithfulness 0.750 — all consistent and reportable. Per-class F1 confirms the "maybe" weakness (0.25) we discussed. Everything checks out.
What this upload tells me about where you are: the zip contains only the four BioBERT files — there's no eval_norag.json or eval_minilm.json. So the baselines haven't been run yet. You have your main system result locked in, but not the comparison controls.
That's the natural next step. Your BioBERT-RAG number (0.532) is meaningful on its own, but it becomes evidence only once you have the two baselines to compare against. Right now you can say "my system scores 0.53"; after the baselines you can say "retrieval adds X points and BioBERT adds Y over a generic embedder" — which is the claim your whole proposal is built on.
A couple of practical things before you run them:
Keep this eval_results.json safe — it's your validated 0.53 BioBERT run. The comparison cell reads it as the "proposed" system, so don't delete it. Maybe copy it to eval_biobert.json as a backup in case a re-run overwrites it.
The MiniLM baseline reuses the exact same 2385 chunks (it reads chunk_meta.json), just re-embedding them with MiniLM — so it's a clean controlled comparison. Good.
The no-RAG baseline is the faster of the two (no retrieval, no faithfulness judge), so run that one first — it's a quick way to get your first comparison point. If no-RAG scores meaningfully below 0.53, you've already got evidence retrieval helps.
One analytical note for your write-up, now that I can see the numbers precisely: your hit rate is 0.88 but accuracy is 0.53. That gap is worth a sentence — it means even when the right evidence is retrieved, the model doesn't always reach the right decision. So your bottleneck is partly generation/reasoning, not just retrieval. That's a useful, honest observation that shows you understand where your system's limits are, and it motivates future work (better/larger LLM, or better prompting) rather than just more retrieval.
Run the two baseline cells when you have GPU time, then Section 17's comparison table. Paste me the output and I'll help you turn it into the results narrative for your dissertation.